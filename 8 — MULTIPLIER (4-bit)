module multiplier4bit(a, b, product);
    input [3:0] a, b;
    output [7:0] product;
    assign product = a * b;
endmodule

TB:
module tb_multiplier4bit;
    reg [3:0] a, b;
    wire [7:0] product;
    multiplier4bit uut(a, b, product);
    initial begin
        a=4'd3; b=4'd4;
        #10 a=4'd7; b=4'd6;
        #10 a=4'd15; b=4'd15;
        #10 $finish;
    end
    initial $monitor("a=%d b=%d | product=%d", a, b, product);
endmodule
